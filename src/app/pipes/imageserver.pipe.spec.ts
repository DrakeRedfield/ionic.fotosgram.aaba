import { ImageserverPipe } from './imageserver.pipe';

describe('ImageserverPipe', () => {
  it('create an instance', () => {
    const pipe = new ImageserverPipe();
    expect(pipe).toBeTruthy();
  });
});
